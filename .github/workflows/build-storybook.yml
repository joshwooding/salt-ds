name: "Build Storybook"
on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build-storybook:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "yarn"
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - name: Save Chromatic branch name
        run: |
          mkdir METADATA
          echo $BRANCH_NAME > ./METADATA/branch-name
        env:
          BRANCH_NAME: ${{ github.event.number || github.ref_name }}
      - uses: actions/upload-artifact@v3
        with:
          name: metadata
          path: ./METADATA
          retention-days: 1
